#ifndef MASKAGLIA_MASKED_H
#define MASKAGLIA_MASKED_H

#include "maskaglia.h"

/*
 * reserved share-aware api
 *
 * do not add an unmasked int16_t output here. the final interface keeps
 * 32 lanes in boolean shares through masked hawk selection.
 */

#endif
